extends ../layout 

block main 
    .profile 
        h1= title 
        .card 
            img(src=`/images/${user.avatar}`)
            h2= user.name 
            p.email= user.email 
            p.bio= user.bio 
        a.button(href="/users") Back