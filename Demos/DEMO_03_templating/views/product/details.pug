extends ../layout 

block main 
    .profile 
    h1= title 
    .card 
        img(src=`/images/${product.image}`)
        h2 #{product.name }
        p #{product.description}
        p #{product.price} €
        p(class=product.available ? "available" : "unavailable") #{product.available ? "Disponible" : "Indisponible"}
    a.button(href="/products") Back